server:
  port: 8082

spring:
  application:
    name: wlhoney-server

eureka:
  instance:
    hostname: wlhoney-server-second
    appname: wlhoney-server-second
    prefer-ip-address: false
  client:
    healthcheck:
      enabled: true
    service-url:
      defaultZone: http://wlhoney-server-first:8081

info:
  app:
    name: wlhoney-server-second